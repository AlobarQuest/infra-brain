# Required
DATABASE_URL=postgresql+asyncpg://infrabrain:changeme@localhost:5432/infrabrain
APP_NAME=infra-brain
APP_ENV=development

# Optional
LOG_LEVEL=INFO
PORT=8000
ALLOWED_HOSTS=localhost

# Docker compose (local dev only — not passed to app)
POSTGRES_PASSWORD=changeme
